package abstracts;

import concrates.GamerManager;

public interface GameManagerServices {
	void add(GamerManager baseGamerService);
	void delete(GamerManager baseGamerService);
	void update(GamerManager baseGamerService);
}
